# Sert index.html par défaut
DirectoryIndex index.html

# Autorise l'accès à tout le contenu
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.html [QSA,L]
</IfModule>
